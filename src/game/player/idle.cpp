#include <iostream>
#include "idle.h"

// void Idle::ShouldTransition() {
//     if (player_->input.x != 0 || player_->input.y != 0) {
//         player_->TransitionTo(new Run(player_));
//     }
// }